# Data normalization
# -- Len and Count
def letterCount(word,letter):
    # Loop through word if i = letter wordCount + 1
    counter = 0
    for x in 'word':
        print(x)
        if x == letter :
            counter += 1
    return counter
letterCount("missisipi", 'i')