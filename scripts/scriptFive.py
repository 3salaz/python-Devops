def reverseWords(array):
    for x in array:
        count = 0
        while count  < 5:
                print(count)
                count += 1  # This is the same as count = count + 1
        arrayIndex = array.index(x)
        return print(array.index(arrayIndex))
inputs = ['words','going','backwards']
reverseWords(inputs)